@using Core.Entities.Platform
@{
    var definition = ViewBag.Definition as AppDefinition;
    ViewData["Title"] = definition?.Name;
}

<div class="container-fluid px-4">
    @* --- УНИФИЦИРОВАННЫЙ ХЕДЕР СТРАНИЦЫ --- *@
    <crm-page-header title="" icon="" subtitle="">
        <a asp-controller="GenericObjects" 
           asp-action="Create" 
           asp-route-entityCode="@definition?.EntityCode" 
           crm-button variant="primary" 
           icon="plus-lg" 
           class="shadow-sm px-4">
            Добавить запись
        </a>
    </crm-page-header>

    @* --- КОНТЕЙНЕР РЕЕСТРА --- *@
    <div crm-card class="overflow-hidden">
        @* На данном этапе реестр отображает только пустое состояние.
           Компонент crm-table-empty автоматически подтянет нужные стили.
        *@
        <crm-table-empty is-search="false" />
    </div>
</div>