@model Core.Entities.Platform.AppDefinition

@{
    ViewData["Title"] = "Удаление сущности";
}

<div class="container-fluid">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2>Удаление сущности</h2>
        <a asp-action="Index" class="btn btn-outline-secondary">
            <i class="bi bi-arrow-left"></i> Назад к списку
        </a>
    </div>

    <div class="card shadow-sm border-danger">
        <div class="card-header bg-danger text-white">
            <h5 class="mb-0">Вы уверены, что хотите удалить эту сущность?</h5>
        </div>
        <div class="card-body">
            <div class="alert alert-warning">
                <i class="bi bi-exclamation-triangle-fill me-2"></i>
                <strong>Внимание!</strong> Это действие удалит сущность "@Model.Name" (@Model.EntityCode) и все связанные с ней поля. Данные восстановить будет невозможно.
            </div>

            <dl class="row mt-4">
                <dt class="col-sm-3 text-muted">Название:</dt>
                <dd class="col-sm-9 fw-bold">@Model.Name</dd>

                <dt class="col-sm-3 text-muted">Системный код:</dt>
                <dd class="col-sm-9"><code>@Model.EntityCode</code></dd>

                <dt class="col-sm-3 text-muted">Раздел меню:</dt>
                <dd class="col-sm-9">
                    @if (Model.Category != null)
                    {
                        <span class="badge bg-light text-dark border">
                            <i class="bi bi-@Model.Category.Icon me-1"></i> @Model.Category.Name
                        </span>
                    }
                    else
                    {
                        <span class="text-muted">Не указан</span>
                    }
                </dd>
            </dl>

            <form asp-action="Delete" class="mt-4">
                <input type="hidden" asp-for="Id" />
                <div class="d-flex gap-2">
                    <button type="submit" class="btn btn-danger px-4">
                        <i class="bi bi-trash me-1"></i> Подтвердить удаление
                    </button>
                    <a asp-action="Index" class="btn btn-light border px-4">Отмена</a>
                </div>
            </form>
        </div>
    </div>
</div>